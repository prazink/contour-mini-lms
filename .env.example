# Local URL is stable; replace the key with `npx supabase status` output.
# For deployment, use the hosted project's URL and publishable key.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-publishable-key
